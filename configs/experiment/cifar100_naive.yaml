defaults:
  - /dataset: cifar100
  - /model: mlp_cifar100
  - /strategy: naive
  - /optimizer: adam
  - /trainer: sequential

experiment_name: cifar100_naive
seed: 42
task_order: ["A", "B", "C", "D", "E"]
