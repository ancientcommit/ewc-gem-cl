defaults:
  - /dataset: mnist_permutations
  - /model: resnet_mnist
  - /strategy: gem
  - /optimizer: adam
  - /trainer: sequential

experiment_name: permmnist_gem_resnet
seed: 42
task_order: ["A", "B", "C", "D", "E"]
