defaults:
  - /dataset: mnist_permutations
  - /model: mlp_mnist
  - /strategy: ewc
  - /optimizer: adam
  - /trainer: sequential

experiment_name: permmnist_ewc_mlp
seed: 42
task_order: ["A", "B", "C", "D", "E"]
